{%- comment -%}
  Minimal stub for css-variables snippet.
  This exists to satisfy includes from templates (e.g., gift_card.liquid)
  without introducing any new design system. Real CSS variables can be
  added here later if the theme requires them.
{%- endcomment -%}

<style>
  :root {
    /* Theme-level CSS custom properties can be defined here if needed. */
  }
</style>

